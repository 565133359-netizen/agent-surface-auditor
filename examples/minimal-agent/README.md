# Minimal Agent Target

This directory is a repository-shaped target used by the project's quick-start example.

It contains only a descriptive agent instruction file. The scanner should report zero findings and exit with code `0` when the default high-severity threshold is used.
