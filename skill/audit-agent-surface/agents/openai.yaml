interface:
  display_name: "Audit Agent Surface"
  short_description: "Triage security boundaries in agentic repositories"
  default_prompt: "Use $audit-agent-surface to scan this agent repository and produce a prioritized remediation plan."
policy:
  allow_implicit_invocation: true
